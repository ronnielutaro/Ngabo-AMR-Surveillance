# Release Tags

Formal Ngabo releases are tagged as:

```text
vMAJOR.MINOR.PATCH
```

Examples:

```text
v0.1.0
v0.2.0
v1.0.0
```

Released tags are immutable. A defect in an existing release requires a new version/tag rather than moving the old tag.
